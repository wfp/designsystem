# Site settings
title: "WFP UI Docs"
email: "me@matthewmorek.com"
description: ""
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site
version: "v0.5.0"
quiet: false

exclude: ["*.sublime-*", "*.yml", "*.DS_*", "_scss"]

# Build settings
markdown: kramdown
highlighter: rouge

kramdown:
  input: GFM

category-list: ["Basics", "Components", "Patterns", "Resources", "Info"]

# Defaults
defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "default"
