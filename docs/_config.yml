# Site settings
title: "WFP UI Docs"
email: "me@matthewmorek.com"
description: ""
baseurl: "/brand/ui" # WFP CDN
url: "https://cdn.wfp.org/" # WFP CDN
version: "v0.4.0-dev"

exclude: ["*.sublime-*", "*.sh", "*.yml", "*.DS_*"]

# Build settings
markdown: kramdown
highlighter: pygments

kramdown:
  input: GFM

category-list: ["Basics", "Components", "Patterns", "Resources", "Info"]

# Defaults
defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "default"