import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Table from './Table';
import ReactDOMServer from 'react-dom/server';

<Meta title="Button" component={Table} />

# Checkbox With `MDX` we can

define a story for `Checkbox` right in the middle of our markdown documentation.

<Preview>
  <Story name="all checkboxes">
    <Table name="Lorem" />
    <form>dddasddasasdds</form>
  </Story>
</Preview>

<Props of={Table} />

Hello you

<div>{ReactDOMServer.renderToString(<Table />)}</div>
