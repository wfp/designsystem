<div class="wfp--theme-light wfp--theme-en">
  <div class="wfp--credits">
    <div class="wfp--credits__info">{{info}}</div>
        {% if image %}
        <img alt="{{img_alt}}" src="{{image}}" />
        {% endif %}
        {{children}}
  </div>
</div>