The **InfoBar** shows very important information on top of the page. Do not use it in other places.

### Usage

`InfoBar` will contain the default links.

```js
import { InfoBar } from '@wfp/ui';
```

```js
<InfoBar pageWidth="narrow" />
```